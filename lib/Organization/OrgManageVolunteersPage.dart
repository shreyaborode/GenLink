import 'package:flutter/cupertino.dart';

class ManageVolunteersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Manage Volunteers'));
  }
}